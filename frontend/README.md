# Frontend (P1)

React + Vite app. Record/upload audio, show waveform, display verdict + on-chain badge.

## Setup
```bash
npm install
npm run dev
```

## TODO
- [ ] Record/upload UI
- [ ] Waveform display (WaveSurfer.js)
- [ ] Call `/analyze` on backend, show mock data first
- [ ] Result screen: verdict, confidence %, "on-chain verified" badge
- [ ] Registry lookup UI (stretch)
